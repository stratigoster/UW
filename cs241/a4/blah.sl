// This is not a valid SL program 
// However, it is syntactically correct.

struct Junk {
	int a;
	int b;
	char c;
	bool a;
}

Junk = b;
b = 4 + 2 - 3 * 4 * 10000 - 91;
a = 4 + 2 + 3 * 3;
a = a + 4;

//int x,y,z;

in >> x+4;
out << 5;

(!(x.y.z.asdl)) | (y & z & x | y);

//(x.y);

hehe (int x, int y) {
	return;
	4 + 2 + 3 * 3;
}

if (me > 10) {
	out << hi;
}
else {
	in >> 4 + 2;
//	hm(x);
};

blah (int y) {
	return;
	int a;
	int b;
	4 + 2 + 3 * 3;
	if (a > b) {
		a = b;
	}
	elseif (b > c) {
		c = d;
		d + c = b;
	}
	elseif (me == you) {
		haha = blah;
		while (me > 10) {
			me = me - 1;

		};
	}
	else {
		red = blue;
		blue = green;
	};
}
string(y);
while (x > 10) {
	x = x + 1;
};

main () {
	4*3-2+1;
	return;
}
